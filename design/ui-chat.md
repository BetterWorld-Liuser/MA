# 聊天区与输入框设计

> 从 [ui.md](ui.md) 延伸：聊天区承载完整用户视图，输入框负责用户与 March 的交互入口。

---

## 聊天区定位

聊天区负责承载用户看到的完整对话历史，同时把当前轮 agent 的推进过程可感知地呈现出来。

目标不是把中栏做成日志终端，而是让用户始终理解三件事：

- 我刚刚发出的请求已经被接收
- AI 当前这轮正在推进，而不是卡住
- 当前轮具体做了哪些关键动作

---

## 基础结构

```text
┌──────────────────────────────────────────────┐
│ March  14:32                                 │
│ 好的，我先看一下现有结构……                     │
│ ┌─ read_file src/auth.rs                    │
│ ├─ replace_lines 12-30                      │
│ └─ reply ✓                                  │
│                                              │
│                           用户  14:32        │
│           帮我把 auth 模块拆成更小的单元       │
│                                              │
│ March  14:33                                 │
│ 已完成，auth 模块拆成了三个文件                │
├──────────────────────────────────────────────┤
│ 输入框                                 [发送] │
└──────────────────────────────────────────────┘
```

- 完整对话历史，用户可以翻回去
- AI 消息固定在左侧，用户消息固定在右侧
- 顶部不放置无信息增益的重复标题

---

## 消息状态模型

用户发送消息后，中栏立刻插入一条新的 AI 消息槽位，经历以下状态：

1. `pending`：用户消息已送出，AI 轮次已创建，尚未产出内容
2. `running`：当前轮正在执行，可能在构建上下文、调用工具、等待模型输出
3. `streaming`：AI 正在流式输出自然语言内容
4. `done`：当前轮完成
5. `error`：当前轮出错，显示错误摘要和可重试入口

---

## 等待态原则

等待态必须建立在真实事件之上，不伪造"AI 正在努力思考"的表演。

**应展示**：简短状态文案（`正在整理上下文`、`正在读取文件`、`正在执行命令`）、轻量动态提示（点状脉冲、弱 shimmer）、当前轮已发生的工具摘要。

**不应展示**：无含义的假进度条、没有事件却持续切换的"思考文案"、把底层细节强行暴露成调试日志。

区分两类等待来源：

| 来源 | 展示方式 |
|------|--------|
| AI 思考中（未产生首 token） | 轻量抽象动态提示 |
| 工具执行中 | 动作名称 + 对象 + 耗时 |

---

## 工具调用展示

- 新工具开始时，立刻在当前 AI 消息下插入一条工具项（实时，不等整轮结束）
- 默认显示紧凑摘要，如 `read_file src/auth.rs`、`run_command cargo test`
- 展开后显示原始参数和输出
- 工具项状态：`running`（spinner + 耗时）→ `success` / `error`

---

## 与右栏联动

- 工具读取文件时，右栏对应监控文件短暂高亮
- 文件写入成功后，右栏新旧感立即刷新
- 新一轮上下文构建完成后，右栏上下文用量同步更新

---

## 实现提示

围绕"轮次事件流"建模，而不是让前端靠零散布尔值拼状态。事件类型与状态边界草案详见 [ui-events.md](ui-events.md)。

---

## 输入框

### 布局

```
┌─────────────────────────────────────────────────────┐
│  帮我重构 [auth.rs ×] 的认证逻辑…                    │
│                                                     │
├─────────────────────────────────────────────────────┤
│  [+]  [⚡ claude-sonnet-4-6 ∨]              [↵发送] │
└─────────────────────────────────────────────────────┘
```

- 输入区自动扩展高度，最高约 6 行后出现内部滚动
- `@` 引用的文件以 chip 形式内联显示，chip 上有 `×` 可移除
- `Enter` 发送，`Shift+Enter` 换行
- 发送后立即清空输入框；当前轮运行时允许用户滚动历史，若停留在底部则自动跟随流式内容

---

### `@` mention

用户输入 `@` 触发 mention 面板，浮于输入框上方：

```
┌──────────────────────────┐
│  > auth                  │  ← fuzzy 搜索
│  src/auth.rs             │  ← 文件
│  src/auth/               │  ← 目录
└──────────────────────────┘
```

两类目标，行为不同：

| 类型 | 行为 |
|------|------|
| `@文件` | 加入 `open_files`，出现在右栏监控区 |
| `@目录` | 注入目录树结构到本条消息，不加入 `open_files` |

**目录树注入规则**：遵守 `.gitignore` 过滤；超过 2,000 行截断，附注 `[目录树共 X 项，已截断至 2000 行]`；只显示路径结构，不展开文件内容。

---

### 模型选择器

```
[⚡ claude-sonnet-4-6 ∨]
   ↓ 点击展开
   ┌──────────────────────────┐
   │ ✓ claude-sonnet-4-6      │
   │   claude-opus-4-6        │
   │   gemini-2.5-pro         │
   │   ──────────────────     │
   │   管理 Provider...       │
   └──────────────────────────┘
```

- 列表来自已配置的 provider，按 provider 分组
- 收起状态优先显示当前已选模型；若当前 task 还未显式选择，则显示该 provider 的当前默认模型；都没有时才显示 `选择模型`
- 展开时优先显示该 provider 最近一次成功读取到的本地缓存列表，再在后台刷新；如果 provider 返回的新列表有变化，再无闪烁地更新
- 每个 session 独立记录选择

---

### `+` 菜单

```
[+] 点击展开
   ┌──────────────────┐
   │  选择文件…       │  → 文件选择器，等同 @文件
   │  选择目录…       │  → 目录选择器，等同 @目录
   └──────────────────┘
```

是 `@` mention 的鼠标操作快捷入口，语义完全相同。

---

## Skills 在右侧面板的展示

Skills 显示在右栏底部，**只读**，用户不能在 UI 上操作：

```
[Skills]
  rust         (已打开)
  git
  api-style
```

- `(已打开)` 表示 AI 已 `open_file` 读取该 skill
- 操作权在 AI，用户通过聊天消息告知即可
